---
description: 提交代码前必须获得用户明确同意，禁止自动 git add / commit
globs: []
alwaysApply: true
---

# Git 提交审批规则

在执行任何 `git add` 或 `git commit` 之前，必须先向用户展示将要提交的内容，并等待用户明确同意。

## 必须遵守的流程

1. 完成代码修改后，**停下来**，向用户说明做了什么改动
2. 询问用户是否确认提交，例如："修改已完成，请确认是否符合预期，是否可以提交？"
3. 只有用户**明确回复同意**（如"提交"、"可以"、"ok"、"确认"等）后，才能执行 `git add` 和 `git commit`
4. 如果用户未回复或回复模糊，**不得**自行提交

## 禁止的行为

- 不得在用户未确认的情况下执行 `git add`
- 不得在用户未确认的情况下执行 `git commit`
- 不得将"帮我修复"、"帮我改一下"等表述理解为提交授权
