---
description: >
  Use when testing, regression-checking, or preparing business review for the
  soil-moisture Agent. Single source of truth is the 30-case formal acceptance
  library at testdata/agent/soil-moisture/case-library.md.
globs: []
alwaysApply: false
---

# Smart Agriculture — 墒情 Agent QA 规则

Follow this rule when the task involves testing, regression, or business review of the soil-moisture Agent.

## 正式入口

- `testdata/agent/soil-moisture/case-library.md`
- `.claude/skills/soil-moisture-qa/SKILL.md`

## 架构约束速查

### 4 Tools

- `query_soil_summary`
- `query_soil_ranking`
- `query_soil_detail`
- `diagnose_empty_result`

### 5 answer_types

- `soil_summary_answer`
- `soil_ranking_answer`
- `soil_detail_answer`
- `guidance_answer`
- `fallback_answer`

### P0 红线

业务问题未调用 Tool 直接回答 → 必须被拦截为 `fallback_answer`（`fallback_reason=tool_missing`）。

## 正式库口径

- 正式 Case 总数固定为 `30`
- 旧三层测试模型已废弃
- 每次都全跑 30 条
- 每条 Case 都保留 `当前回答`
- 每条业务 Case 都必须有：
  - `数据库校验断言`
  - `是否符合事实`

## 当前正式分布

| 类型 | 数量 |
|------|----:|
| `guidance_answer` | 8 |
| `soil_summary_answer` | 6 |
| `soil_ranking_answer` | 4 |
| `soil_detail_answer` | 8 |
| `fallback_answer` | 4 |

## 执行要求

- 优先验证答案是否被真实数据支撑，而不是只看文案是否像样
- 不允许继续沿用旧 `130` Case、旧 CaseID、旧三层测试模型
- 新增或修订 Case 时，只改 `case-library.md`
