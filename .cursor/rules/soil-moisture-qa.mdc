---
description: >
  Use when testing, regression-checking, or preparing business review for the
  soil-moisture Agent. Single source of truth is the 56-case formal acceptance
  library at testdata/agent/soil-moisture/case-library.md.
globs: []
alwaysApply: false
---

# Smart Agriculture — 墒情 Agent QA 规则

Follow this rule when the task involves testing, regression, or business review of the soil-moisture Agent.

## 正式入口

- `testdata/agent/soil-moisture/case-library.md`
- `.claude/skills/soil-moisture-qa/SKILL.md`

## 架构约束速查

### 4 业务 Tools

- `query_soil_summary`
- `query_soil_ranking`
- `query_soil_detail`
- `query_soil_comparison`（横向对比，2026 Q2 加入）

> 空结果由系统在 `query_soil_*` 返回空时自动产出 `empty_result_path`，不再调用独立诊断工具。

### 5 answer_types

- `soil_summary_answer`
- `soil_ranking_answer`
- `soil_detail_answer`
- `guidance_answer`
- `fallback_answer`

### 6 fallback_reason（必须全覆盖）

- `no_data` / `entity_not_found` / `tool_missing` / `tool_blocked` / `fact_check_failed` / `unknown`

### entity_confidence

- `low` → ParameterResolver 拦截，不查库
- `medium` → 继续查询，回答须附"置信度中"说明
- `high` → 正常查询

### P0 红线

业务问题路径必须存在 Tool 调用与真实数据；两个对偶失败：
- `tool_missing`：业务问题但 `tool_trace=[]` → fallback
- `tool_blocked`：调用了 Tool 但执行失败 → 由 `FallbackGuard` 兜底

## 正式库口径

- 正式 Case 总数固定为 `56`
- 每次都全跑 56 条
- 顶层问法变体、轻量错字、路由冲突优先补到 `TurnRouteDecisionService` 路由矩阵单测，不直接扩正式 56 条
- 每条 Case 都保留 `当前回答`
- 每条业务 Case 都必须有：
  - `数据库校验断言`
  - `是否符合事实`

## 当前正式分布

| 章节 | 数量 |
|------|----:|
| Guidance Cases | 15 |
| Summary Cases | 10 |
| Ranking Cases | 8 |
| Detail Cases | 13 |
| Fallback Cases | 10 |

## 执行要求

- 优先验证答案是否被真实数据支撑，而不是只看文案是否像样
- 只允许使用当前正式库中的 56 条 Case 与当前 `SM-*` CaseID 体系
- 快速回归可先运行：
  - `PYTHONPATH=apps/agent:apps/agent/tests .venv/bin/python -m unittest apps.agent.tests.test_turn_route_decision_service_unittest apps.agent.tests.test_turn_route_query_shape_matrix_unittest -v`
- 新增或修订 Case 时，只改 `case-library.md`
- 当用户要求"正式测试报告""逐条详细报告""56 条 Case 全量验收报告"时，必须逐条覆盖 56 条 Case，并为每条业务 Case 输出：
  - 实际输入 / 输出
  - 实际 Tool 与参数
  - `tool_trace / query_result / answer_facts / query_log_entries`
  - `执行 SQL` 或 `等效 SQL`
  - SQL 结果摘要与样本
  - 数据库事实校验结果
  - `是否符合事实`
  - 最终通过 / 失败结论
