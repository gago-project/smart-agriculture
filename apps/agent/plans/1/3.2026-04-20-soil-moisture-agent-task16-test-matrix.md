# 3. Task 16：墒情 Agent 验收测试样例矩阵（通关版）

> 目的：这份矩阵不是“补充说明”，而是研发、测试、产品联调时的通关清单。  
> 原则：每个 Case 都要能落到明确的 `input_type / intent / slots / query_type / SQL 模板 / answer_type / 规则触发 / 是否写日志`。  
> 范围：仅覆盖当前“墒情 Agent MVP”能力，不覆盖虫害、气象、正式预警发布闭环。

---

## 1. 测试前置假设

- 默认时区：`Asia/Shanghai`
- 默认库内最新业务时间：`2026-04-20 00:00:00`
- 默认已存在有效地区：
  - `南通市`
  - `如东县`
  - `镇江市`
  - `镇江经开区`
- 默认地区解析支持静态 alias 种子：
  - `南京 -> 南京市`
  - `南通 -> 南通市`
  - `如东 -> 如东县`
- 默认地区解析支持轻度模糊兜底，但只允许**唯一高置信**的一编辑距离命中；多候选必须澄清
- 默认已存在有效设备：
  - `SNS00204333`
  - `SNS00213807`
- 默认存在最近导入批次，且 `fact_soil_moisture.batch_id` 已正确关联 `etl_import_batch.batch_id`
- 默认所有事实查询只查当前有效数据
- 默认 `ExecutionGate` 生效：
  - 排名类最大时间窗 `365` 天
  - 异常类最大时间窗 `180` 天
  - 详情/趋势类最大时间窗 `90` 天
  - `TopN` 最大值 `20`
  - 不允许无约束全省全表扫描
  - 不允许批量设备趋势查询

---

## 2. 字段说明

| 列名 | 说明 |
|---|---|
| `CaseID` | 测试编号 |
| `用户输入` | 当前轮用户问题 |
| `上下文` | 多轮情况下的上一轮或历史有效上下文 |
| `预期 input_type` | 输入分类 |
| `预期 intent` | 业务意图 |
| `预期 slots` | 关键槽位，不要求写全量 JSON |
| `预期 query_type / SQL` | 应落到哪个查询类型、哪个 SQL 模板 |
| `ETG` | `ExecutionGate` 结果：`pass / clarify / block` |
| `预期 answer_type` | 最终回答类型 |
| `规则触发` | `yes / no / n/a` |
| `写查询日志` | 是否写 `agent_query_log` |
| `关键断言` | 验收重点 |

---

## 3. 验收测试矩阵（44 个典型 Case）

### A. 非业务 / 安全提示（3 个）

| CaseID | 用户输入 | 上下文 | 预期 input_type | 预期 intent | 预期 slots | 预期 query_type / SQL | ETG | 预期 answer_type | 规则触发 | 写查询日志 | 关键断言 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| SH-01 | 你好 | 无 | `greeting` | 无 | 无 | 无 | `pass` | `safe_hint_answer` | `n/a` | 否 | 不查库；回复能力范围与示例问题 |
| SH-02 | 你能做什么 | 无 | `capability_question` | 无 | 无 | 无 | `pass` | `safe_hint_answer` | `n/a` | 否 | 不查库；列出墒情查询、异常分析、模板输出能力 |
| SH-03 | ？？？ | 无 | `meaningless_input` | 无 | 无 | 无 | `pass` | `safe_hint_answer` | `n/a` | 否 | 不查库；不答非所问 |

### B. 澄清引导（3 个）

| CaseID | 用户输入 | 上下文 | 预期 input_type | 预期 intent | 预期 slots | 预期 query_type / SQL | ETG | 预期 answer_type | 规则触发 | 写查询日志 | 关键断言 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| CL-01 | 看看 | 无 | `ambiguous_low_confidence` | `clarification_needed` | 无 | 无 | `pass` | `clarification_answer` | `n/a` | 否 | 不查库；给出 4~5 个可选问题方向 |
| CL-02 | 帮我查一下 | 无 | `ambiguous_low_confidence` | `clarification_needed` | 无 | 无 | `pass` | `clarification_answer` | `n/a` | 否 | 不默认继承任何隐含意图 |
| CL-03 | 那个情况呢 | 无有效业务上下文 | `ambiguous_low_confidence` | `clarification_needed` | 无 | 无 | `pass` | `clarification_answer` | `n/a` | 否 | 因缺上下文，不能硬继承 |

### C. 墒情概览（4 个）

| CaseID | 用户输入 | 上下文 | 预期 input_type | 预期 intent | 预期 slots | 预期 query_type / SQL | ETG | 预期 answer_type | 规则触发 | 写查询日志 | 关键断言 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| SU-01 | 最近墒情怎么样 | 无 | `business_direct` | `soil_recent_summary` | `time=最近7天` | `recent_summary / SQL-01` | `pass` | `soil_summary_answer` | `no` | 是 | 内部以库内最新业务时间为基准，不用系统当前时间；对外默认不展示样本数、最新业务时间、数据来源 |
| SU-02 | 这批数据整体情况如何 | 无 | `business_direct` | `soil_recent_summary` | `batch_id=latest_batch` | `recent_summary / SQL-01` | `pass` | `soil_summary_answer` | `no` | 是 | “这一批”优先按 `batch_id` 查，不只靠 `source_file` |
| SU-03 | 南通市最近7天墒情怎么样 | 无 | `business_direct` | `soil_recent_summary` | `city_name=南通市; time=近7天` | `recent_summary / SQL-01` | `pass` | `soil_summary_answer` | `no` | 是 | 回答应突出地区概况与关注点，不暴露样本数、最新业务时间、数据来源等内部统计口径 |
| SU-04 | 现在的墒情 | 无 | `business_colloquial` | `soil_recent_summary` | `time=最新业务时间` | `SQL-07 场景4 -> SQL-01` | `pass` | `soil_summary_answer` | `no` | 是 | 先查库内最新业务时间，再汇总；不能硬查当天；对外不必直接展示该内部基准时间 |

### D. 排名对比（5 个）

| CaseID | 用户输入 | 上下文 | 预期 input_type | 预期 intent | 预期 slots | 预期 query_type / SQL | ETG | 预期 answer_type | 规则触发 | 写查询日志 | 关键断言 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| RK-01 | 过去一个月哪里最严重 | 无 | `business_direct` | `soil_severity_ranking` | `time=30天; aggregation=county` | `severity_ranking / SQL-02` | `pass` | `soil_ranking_answer` | `no` | 是 | 默认按县区排名；对外只给综合风险顺序，不直接暴露 `soil_anomaly_score` 等内部字段名 |
| RK-02 | 前10个最严重设备 | 无 | `business_direct` | `soil_severity_ranking` | `top_n=10; aggregation=device` | `severity_ranking / SQL-02` | `pass` | `soil_ranking_answer` | `no` | 是 | 返回设备 Top10，不超过 10 条；不输出“异常分”等内部实现词汇 |
| RK-03 | 哪个市最严重 | 无 | `business_direct` | `soil_severity_ranking` | `aggregation=city` | `severity_ranking / SQL-02` | `pass` | `soil_ranking_answer` | `no` | 是 | 应走市级聚合，不应默认仍按县区返回；对外不暴露内部排序实现细节 |
| RK-04 | 给我前100个最严重设备 | 无 | `business_direct` | `soil_severity_ranking` | `top_n=100` | 无 | `clarify` | `clarification_answer` | `n/a` | 否 | 命中 `max_top_n=20`；提示是否改查前 20 |
| RK-05 | 全省近三年所有设备排名 | 无 | `business_direct` | `soil_severity_ranking` | `time=3年; aggregation=device` | 无 | `block` | `clarification_answer` | `n/a` | 否 | 命中时间窗超限 + 大范围扫描限制，不得继续查库 |

### E. 地区 / 设备详情（5 个）

| CaseID | 用户输入 | 上下文 | 预期 input_type | 预期 intent | 预期 slots | 预期 query_type / SQL | ETG | 预期 answer_type | 规则触发 | 写查询日志 | 关键断言 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| DT-01 | 如东县最近怎么样 | 无 | `business_direct` | `soil_region_query` | `county_name=如东县; time=最近` | `region_detail / SQL-03` | `pass` | `soil_detail_answer` | `yes` | 是 | 包含最新记录、20cm 水分、状态判断；不要求对外展示来源文件 |
| DT-02 | SNS00204333 最近有没有异常 | 无 | `business_direct` | `soil_device_query` | `device_sn=SNS00204333; time=最近` | `device_detail / SQL-03` | `pass` | `soil_detail_answer` | `yes` | 是 | 能正确给出该设备的最新事实与状态判断，不把规则判断写成自由发挥 |
| DT-03 | 那上周的呢 | 上一轮已确认 `county_name=如东县` | `business_colloquial` | `soil_region_query` | `county_name=继承如东县; time=上周` | `region_detail / SQL-03` | `pass` | `soil_detail_answer` | `yes` | 是 | 正确继承地区，只重算时间范围 |
| DT-04 | 换成20cm看 | 上一轮设备详情上下文有效 | `business_colloquial` | `soil_device_query` | `device_sn=继承; metric=water20cm` | `device_detail / SQL-03` | `pass` | `soil_detail_answer` | `yes` | 是 | 只继承白名单槽位，不继承无关输出意图 |
| DT-05 | 有没有问题 | 距离上次明确对象已 4~5 轮 | `business_colloquial` | `clarification_needed` | 无 | 无 | `pass` | `clarification_answer` | `n/a` | 否 | 命中上下文衰减；不能继续强继承旧对象 |

### E2. 地区别名 / 轻度模糊解析（5 个）

| CaseID | 用户输入 | 上下文 | 预期 input_type | 预期 intent | 预期 slots | 预期 query_type / SQL | ETG | 预期 answer_type | 规则触发 | 写查询日志 | 关键断言 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| RG-01 | 南京最近一个月的数据 | 无 | `business_direct` | `soil_region_query` | `city_name=南京市; time=30天` | `region_detail / SQL-03` | `pass` | `soil_detail_answer` | `yes` | 是 | `南京` 必须补全为 `南京市`，不能因为简称未命中而退回默认概览 |
| RG-02 | 南通最近7天墒情怎么样 | 无 | `business_direct` | `soil_recent_summary` | `city_name=南通市; time=近7天` | `recent_summary / SQL-01` | `pass` | `soil_summary_answer` | `no` | 是 | 城市简称补全后，仍保持原有概览路由，不额外改意图 |
| RG-03 | 如东最近怎么样 | 无 | `business_direct` | `soil_region_query` | `county_name=如东县; city_name=南通市; time=最近` | `region_detail / SQL-03` | `pass` | `soil_detail_answer` | `yes` | 是 | 县级简称补全后需补齐父级市，不允许只落裸 `county_name` |
| RG-04 | 苏洲最近一个月的数据 | 无 | `business_direct` | `soil_region_query` | `city_name=苏州市; time=30天` | `region_detail / SQL-03` | `pass` | `soil_detail_answer` | `yes` | 是 | 轻度模糊只允许唯一高置信候选自动补全 |
| RG-05 | 新区最近怎么样 | 无 | `business_direct` | `clarification_needed` | `ambiguity_candidates=多个地区` | 无 | `pass` | `clarification_answer` | `n/a` | 否 | 多候选 alias 或模糊命中时，必须澄清，不能直接执行 SQL |

### F. 异常分析（4 个）

| CaseID | 用户输入 | 上下文 | 预期 input_type | 预期 intent | 预期 slots | 预期 query_type / SQL | ETG | 预期 answer_type | 规则触发 | 写查询日志 | 关键断言 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| AN-01 | 最近有没有异常 | 无 | `business_direct` | `soil_anomaly_query` | `time=最近` | `anomaly_list / SQL-04` | `pass` | `soil_anomaly_answer` | `yes` | 是 | SQL 只拉候选；最终异常统计以规则复判结果为准，对外不直接展示规则引擎名称 |
| AN-02 | 哪些地方需要关注 | 无 | `business_direct` | `soil_anomaly_query` | `time=最近` | `anomaly_list / SQL-04` | `pass` | `soil_anomaly_answer` | `yes` | 是 | 输出需含异常数量和重点关注对象，不暴露内部规则链路说明 |
| AN-03 | 有没有重旱 | 无 | `business_direct` | `soil_anomaly_query` | `status_target=heavy_drought` | `anomaly_list / SQL-04` | `pass` | `soil_anomaly_answer` | `yes` | 是 | “重旱”判断必须来自规则引擎，但对外回答不直接点名内部规则组件 |
| AN-04 | 查过去5年异常点位 | 无 | `business_direct` | `soil_anomaly_query` | `time=5年` | 无 | `clarify` | `clarification_answer` | `n/a` | 否 | 命中异常查询最大时间窗 180 天 |

### G. 预警模板输出（4 个）

| CaseID | 用户输入 | 上下文 | 预期 input_type | 预期 intent | 预期 slots | 预期 query_type / SQL | ETG | 预期 answer_type | 规则触发 | 写查询日志 | 关键断言 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| WA-01 | 生成一条墒情预警 | 无 | `business_direct` | `soil_warning_generation` | `need_template=true` | `latest_record / SQL-05` | `pass` | `soil_warning_answer` | `yes/no` | 是 | 必须先规则判断，再决定是否触发模板输出 |
| WA-02 | SNS00204333 需要发预警吗 | 无 | `business_direct` | `soil_warning_generation` | `device_sn=SNS00204333` | `latest_record / SQL-05` | `pass` | `soil_warning_answer` | `no` | 是 | 未触发时内部应为 `soil_status=not_triggered; warning_level=none` |
| WA-03 | 按模板输出 SNS00213807 最新预警 | 无 | `business_direct` | `soil_warning_generation` | `device_sn=SNS00213807; render_mode=strict` | `latest_record / SQL-05` | `pass` | `soil_warning_answer` | `yes/no` | 是 | `strict_mode` 只能输出模板正文 + 极少量固定补充 |
| WA-04 | 按模板输出并解释原因 | 无 | `business_direct` | `soil_warning_generation` | `render_mode=plus_explanation` | `latest_record / SQL-05` | `pass` | `soil_warning_answer` | `yes/no` | 是 | 模板正文与解释段必须分区输出，不能揉成一段 |

### H. 指标解释 / 指导建议（3 个）

| CaseID | 用户输入 | 上下文 | 预期 input_type | 预期 intent | 预期 slots | 预期 query_type / SQL | ETG | 预期 answer_type | 规则触发 | 写查询日志 | 关键断言 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| AD-01 | 83.18 是什么意思 | 上一轮已有设备详情 | `business_colloquial` | `soil_metric_explanation` | `metric=water20cm` | `latest_record / SQL-06` | `pass` | `soil_advice_answer` | `no` | 是 | 先给数据事实，再解释指标含义 |
| AD-02 | 这种情况农户要注意什么 | 上一轮已有异常结果 | `business_colloquial` | `soil_management_advice` | `audience=farmer` | `latest_record / SQL-06` | `pass` | `soil_advice_answer` | `no` | 是 | 建议必须带不确定性说明 |
| AD-03 | 这种情况大棚怎么处理 | 上一轮已有异常结果 | `business_colloquial` | `soil_management_advice` | `audience=greenhouse` | `latest_record / SQL-06` | `pass` | `soil_advice_answer` | `no` | 是 | 输出对象应切换为设施大棚主体 |

### I. 无数据 / 找不到 / 兜底（3 个）

| CaseID | 用户输入 | 上下文 | 预期 input_type | 预期 intent | 预期 slots | 预期 query_type / SQL | ETG | 预期 answer_type | 规则触发 | 写查询日志 | 关键断言 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| FB-01 | XX乡镇最近怎么样 | 无 | `business_direct` | `soil_region_query` | `town_name=XX乡镇` | `fallback / SQL-07` | `pass` | `fallback_answer` | `n/a` | 是 | 地区不存在时不能编结果，要提示核对名称 |
| FB-02 | 如东县 2026-04-15 有数据吗 | 无 | `business_direct` | `soil_region_query` | `county_name=如东县; time=2026-04-15` | `fallback / SQL-07` | `pass` | `fallback_answer` | `n/a` | 是 | 指定日期无数据时，要提示库内最新业务时间 |
| FB-03 | 这一批里 SNS00299999 有没有数据 | 无 | `business_direct` | `soil_device_query` | `batch_id=latest_batch; device_sn=SNS00299999` | `fallback / SQL-07` | `pass` | `fallback_answer` | `n/a` | 是 | 设备不存在或批次内无数据时，不得回答“正常” |

### J. 能力边界（2 个）

| CaseID | 用户输入 | 上下文 | 预期 input_type | 预期 intent | 预期 slots | 预期 query_type / SQL | ETG | 预期 answer_type | 规则触发 | 写查询日志 | 关键断言 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| BO-01 | 查一下明天天气 | 无 | `out_of_domain` | `out_of_scope` | 无 | 无 | `pass` | `boundary_answer` | `n/a` | 否 | 明确只支持墒情业务，不查天气 |
| BO-02 | 帮我写首诗 | 无 | `out_of_domain` | `out_of_scope` | 无 | 无 | `pass` | `boundary_answer` | `n/a` | 否 | 不转成闲聊，不误走业务链路 |

### K. 门禁与生产保护专项（3 个）

| CaseID | 用户输入 | 上下文 | 预期 input_type | 预期 intent | 预期 slots | 预期 query_type / SQL | ETG | 预期 answer_type | 规则触发 | 写查询日志 | 关键断言 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| PG-01 | 全省近一年每天趋势 | 无 | `business_direct` | `soil_region_query` | `aggregation=province; trend=daily` | 无 | `clarify` | `clarification_answer` | `n/a` | 否 | 趋势查询必须带地区或设备约束，不能直接全省日趋势 |
| PG-02 | 查所有设备最近90天趋势 | 无 | `business_direct` | `soil_device_query` | `batch_devices=all` | 无 | `block` | `clarification_answer` | `n/a` | 否 | 不允许批量设备趋势查询 |
| PG-03 | 过去两年镇江市异常概况 | 无 | `business_direct` | `soil_anomaly_query` | `city_name=镇江市; time=2年` | 无 | `clarify` | `clarification_answer` | `n/a` | 否 | 当前实现直接澄清，不自动缩窗执行 |

---

## 4. 必须重点抽查的 10 个断言

- 非业务输入不得触发 SQL。
- `safe_hint_answer`、`clarification_answer`、`boundary_answer` 默认不写 `agent_query_log`。
- “现在 / 当前 / 最新”必须先取库内最新业务时间。
- “这一批”必须优先按 `batch_id` 解析。
- 异常 SQL 只拉候选，最终异常统计必须以 `RuleEngine` 结果为准。
- 概览回答默认不直接暴露样本数、最新业务时间、数据来源。
- 排名回答默认不直接暴露 `soil_anomaly_score`、异常分或内部排序字段名。
- 异常回答默认不直接点名 `SoilStatusRuleEngine` 等内部规则组件。
- `soil_warning_answer` 未命中时，内部返回应是 `soil_status=not_triggered`、`warning_level=none`。
- 模板 `strict_mode` 下，正文不能被自由改写。
- 上下文继承只允许白名单槽位，且最近 1~2 轮高置信，3~5 轮弱继承。
- `ExecutionGate` 超限后不得继续调用 `SoilDataQueryTool`。
- 数字、时间、地区、设备号必须通过 `DataFactChecker`，不能漂移。

---

## 5. 建议执行方式

- 第一轮：先跑 32 个逻辑 Case，验证 `input_type / intent / answer_type / SQL 模板` 是否正确。
- 第二轮：对其中需要查库的 Case，补真实数据断言，检查数值与模板输出是否一致。
- 第三轮：专项抽查 5 类高风险问题：
  - 多轮继承
  - 无数据兜底
  - 模板严格模式
  - ExecutionGate 超限拦截
  - DataFactChecker 数值校验

一句话结论：

> 如果这 32 个 Case 都能稳定通过，这个墒情 Agent 的 MVP 就已经具备很强的可交付性。
